# Intentionally created for dispatch/monitor test
name: fail1 (dispatch-test)
on:
  workflow_dispatch:
jobs:
  succeed:
    runs-on: ubuntu-latest
    steps:
      - name: Print success
        run: |
          echo "This is a dispatch test workflow that should succeed."
          exit 0
