# Intentionally failing workflow: invalid action ref to cause action resolution failure
name: CI Fail Action
on:
  workflow_dispatch:
jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (fixed ref)
        uses: actions/checkout@v4
      - name: Echo
        run: echo "This step will run after checkout succeeds"
