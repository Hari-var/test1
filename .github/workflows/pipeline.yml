name: Checkout Failure Demo

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - run: echo "This will execute now."
