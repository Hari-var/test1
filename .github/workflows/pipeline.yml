name: Checkout Failure Demo

on:
  workflow_dispatch:

jobs:
  checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Wrong Repository
        uses: actions/checkout@v4
        with:
          repository: Raghava/repo-does-not-exist
          token: ${{ secrets.GITHUB_TOKEN }}

      - run: echo "This will never execute."
